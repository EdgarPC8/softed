import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      light: "#009688",
      main: "#33ab9f",
      dark: "#00695f",
      contrastText: "#fff",
    
    },
    secondary: {
      light: "#1de9b6",
      main: "#14a37f",
      dark: "#14a37f",
      contrastText: "#000",
    },
  },
});

export default theme;
